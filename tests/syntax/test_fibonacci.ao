fib = f (n) {
    if (n == 1) {
        return 1;
    } elif (n == 2) {
        return 2;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
};

n = 1;
while (n < 20) {
    print fib(n);
    n = n + 1;
}
